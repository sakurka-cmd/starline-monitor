FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application
COPY worker.py .
COPY config.json .

# Run as daemon (continuous polling)
CMD ["python", "worker.py", "-c", "config.json", "--daemon"]
